# documentation
springdoc.api-docs.path=/api
springdoc.swagger-ui.path=/swagger.html

# validation messages
server.error.include-binding-errors=always
server.error.include-message=always

# jdbc
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.datasource.url=jdbc:${DB_DIALECT}://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

##ssl
server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=ionos
server.ssl.keyStoreType=PKCS12
server.ssl.keyAlias=tomcat

# jwt
security.jwt.secret-key=nsdmlwe43lscdhqw32scdlkqdswlmarbirqwb34z87cabsqe212312s/$%hj213dasj)GH23KMLCSDXASghad3054HGYXC

#minio
minio.endpoint=http://${BUCKET_HOST}:${BUCKET_PORT}
minio.access-key=${BUCKET_ACCESS_KEY}
minio.secret-key=${BUCKET_ACCESS_SECRET}
minio.bucket-name=${BUCKET_NAME}